---
import Container from "../../components/utils/Container.astro";
import Layout from "../../layouts/Layout.astro";

import { EMAIL, APP_NAME } from "../../consts";
---

<Layout>
  <Container className="text-white dark:text-primary-100 px-0">
    <h1 class="text-3xl font-bold mb-4">Soporte de {APP_NAME}</h1>
    <p class="mb-4">
      Si tienes alguna pregunta o necesitas ayuda con la aplicación, no dudes en
      contactarnos a través de nuestro correo electrónico:
      <a class="hover:underline text-primary-200" href={`mailto:${EMAIL}`}
        >{EMAIL}</a
      >
    </p>
    <div class="support-options mt-10">
      <a
        class="p-3 bg-primary-600 dark:bg-primary-500 text-red-800 inline-block rounded-lg hover:shadow-xl transition-shadow"
        href="/soporte/eliminar-cuenta">Eliminar cuenta</a
      >
    </div>
  </Container>
</Layout>
